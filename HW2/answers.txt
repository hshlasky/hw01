1. We reused about 10 lines of code in every file. We could have written a file that holds these lines and import this file in each code.

2. No. Because 100 is a too small number and isn't representive enough. If we would have tested a large number of grades, we would have expected them to be uniformly distributed.
